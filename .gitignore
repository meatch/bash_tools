.DS_Store
.claude

# Personal config — each developer maintains their own
config.sh
config/*.sh
!config/*.sample.sh

# Personal/unversioned scripts — drop .sh files here to auto-source on shell init
local/*.sh
!local/*.sample.sh